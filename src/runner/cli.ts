import { $ } from "bun";

// this just runs CLI execute
export const execute = (project, workflow, options) => {
  // build the command
};

// run commands through the CLI
// uses bun shell
export const merge = (source, target, options) => {};
